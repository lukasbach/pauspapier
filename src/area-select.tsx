import { Area } from './common';
import React from 'react';

export const AreaSelect: React.FC<{ screenshotPath: string, onSelectArea: (area: Area) => void }> = props => {
  return (
    <>
      Area Select
    </>
  );
};